{
  "name": "codex-safe-scaffold",
  "version": "1.0.0",
  "main": "dist/server/index.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"cd client && vite\" \"ts-node server/index.ts\"",
    "build": "tsc && cd client && vite build",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "vite": "^5.0.11",
    "concurrently": "^8.0.1"
  }
}